//import javax.swing.JOptionPane;

public class Main {

    public static void main(String[] args) {
        // AVehicle mycar = new AVehicle();

        // mycar.setNum_of_tyres(Integer.parseInt(JOptionPane.showInputDialog("Enter number of tyres:")));
        // mycar.setCar_make(JOptionPane.showInputDialog("Enter the make of your car"));
        // mycar.setCare_model(JOptionPane.showInputDialog("Enter the model name for " + mycar.getCar_make()));
        // mycar.setNumber_plate(Integer.parseInt(JOptionPane.showInputDialog("Enter the number plate of your car")));


        // mycar.BuildACar();

        TaskAllowance input = new TaskAllowance();
        input.setIncome(500);
        input.getIncome();

        input.CalculateTax();


    }
}
